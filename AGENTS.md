# Agent 协作约定

## 先读
先读 README.md 和 docs/HANDOFF.md，再检查 `git status --short`、当前分支与远端。不要覆盖其他 Agent 或用户未提交的改动。

## 项目边界
- 本项目是马达加斯加 2026-09-30 至 2026-10-09 的静态旅行读本。
- 当前发布目标为 GitHub Pages。`.openai/hosting.json` 仅保留旧 Sites 标识，不得因其存在自动发布到 Sites；除非用户另行要求。
- `dist/` 是手工维护的正式源码，**不是可删除的构建缓存**。禁止清空、重新生成或删除整个目录。
- 不删除用户文件、不强推、不重置或覆盖他人改动；冲突先检查并保留双方内容。
- 不上传密钥、证件、联系方式、原始私密行程截图或其他未授权个人材料。

## 修改职责
- 内容研究：dist/content.js；路线研究：dist/routes.js；界面：dist/index.html、dist/style.css、dist/app.js。
- 若用户要求多个 Agent 并行，由主 Agent 先分配不重叠的文件。所有 Agent 均须报告修改文件、验证结果和未解决问题。
- 仅集成负责人提交、推送与发布。其他 Agent 不操作远端、发布分支或账号配置。
- 跨文件改动先协调，禁止相互覆盖。不得用额外创建任务代替用户当前任务。

## 内容质量
- 区分确定地点与推定地点，不将候选地点加入确定导航。
- 保留来源、图片署名、授权信息和不确定性。不可把示意图称为 GPS 路线或 Google 地图截图。
- 新增图片放在 dist/images，使用相对路径，并提供 alt、caption、来源与许可。
- 当前不含网页内编辑、上传、后台数据库；不要宣称已有这些功能。

## 验证与交付
运行 `node scripts/validate.mjs`；界面变更检查日期切换、来源展开和手机布局。部署时按 docs/HANDOFF.md，先推送源码，再发布 dist 子树。确认 Pages 构建成功及实际 URL 返回正常后，才能称为发布完成。
交付时记录改动、验证、部署结果和仍待确认的事项。不要为文档修改无意义地反复发布。
